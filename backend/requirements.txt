# SignalScout backend dependencies
# Direct dependencies only; pip resolves the rest.
# Install with: pip install -r requirements.txt

# Web framework + CORS
Flask==3.1.0
Flask-Cors==5.0.0

# WSGI server for production (not used by the local dev server).
# Linux-only wheel; on Windows this is skipped so local installs still work.
gunicorn==23.0.0; sys_platform != "win32"

# Realtime price push. Needs a host that holds long-lived connections
# (Render does; serverless platforms do not).
Flask-SocketIO==5.4.1
python-socketio==5.11.4
simple-websocket==1.1.0

# Database (SQLAlchemy ORM + Postgres driver)
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10

# Config / env
python-dotenv==1.0.1

# Auth
bcrypt==4.2.1
PyJWT==2.10.1

# Market data + numerics
yfinance==0.2.52
pandas==2.2.3
numpy==2.2.1

# News scraping
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0

# Sentiment (news headline polarity)
textblob==0.18.0.post0

# AI agent (proposal generation). Optional at runtime: without ANTHROPIC_API_KEY
# the agent routes return 503 and the rest of the app is unaffected.
anthropic==0.120.2

# Charting utilities used by app.py imports
matplotlib==3.10.0
